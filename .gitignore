# Editor / OS
.DS_Store
Thumbs.db
*.tmp
*.swp
.vscode/
.idea/

# Logs
*.log

# Env (git-secrets defense-in-depth; no .env expected in this static site)
.env*

# Dependencies (defensive — this project has no runtime deps)
node_modules/
# Agent / methodology tooling (per-machine, no es parte del sitio)
.atl/
.opencode/
interstellar/.specify/extensions/

# Material fuente crudo de assets (clips, frames de referencia, etc.): local.
# Los assets FINALES (optimizados) si se versionan en assets/img/.
interstellar/assets/_source/
# Volcados completos de galerias de fotogramas (FILMGRAB "download all",
# cap-that.com, etc.): material de referencia, pesado, local. Los frames que
# SE usan van optimizados a img/ (y con su fila en CREDITOS.md).
interstellar/assets/photo-gallery-*/
interstellar/assets/cap-that.com*/
